<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hello.capstone.repository.ItemMapper">

	<insert id="saveitem">
		insert into item(shopidx, itemname, cost, salecost, quantity, image, category, starttime, endtime) values(#{shopidx},#{itemname},#{cost},#{salecost},#{quantity},#{image},#{category}, #{starttime}, #{endtime});
	</insert>

	<select id="findByShopIdx_itemname" resultType="item">
		select * from item where shopidx = #{shopidx} and itemname = #{itemname};
	</select>

</mapper>